import 'package:flutter/material.dart';

class ModuleDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Module details'),
      ),
      body: Container(
        child: Text('Detail module'),
      ),
    );
  }
}
